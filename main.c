#include"logic.c"
int main() {
	/*int arr[] = {6, 3, 8, 4, 9, 2, 7, 1, 5, 0};
	int s = sizeof(arr) / sizeof(arr[0]);
	HeapSort(arr, s);
	for(int i = 0; i < s; i++) {
		printf("%d\t", arr[i]);
	}
	printf("\n");*/
	return 0;
}
